package StarCraftII_Account

// AUTOMATICALLY GENERATED FILE, DO NOT EDIT
// This file is generated by tools/updater
// Author: @Thenecromance

import (
	"context"
	"encoding/json"
	

	

	"io"
	"net/http"

	"github.com/Thenecromance/BlizzardAPI/ApiError"
	"github.com/Thenecromance/BlizzardAPI/api/Authentication"
	"github.com/Thenecromance/BlizzardAPI/global"
	"github.com/Thenecromance/BlizzardAPI/utils"


	"github.com/jtacoma/uritemplates"

)


// ==============================================================================================
// API: Player
// ==============================================================================================

type PlayerFields struct {
	AccountId string `uri:"accountId" binding:"required"` // The ID of the account for which to retrieve data.
	
	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookPlayer allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookPlayer func(context.Context, *PlayerFields) (any, error)

// StringPlayer executes the request and returns the raw response body string.
func StringPlayer(ctx context.Context, fields *PlayerFields) (string, error) {
	if fields == nil {
		fields = &PlayerFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("Player")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/sc2/player/:accountId",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"accountId": fields.AccountId,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	resp, err := Authentication.Client().Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgePlayer routes the request to either CN or Global logic based on input.
func bridgePlayer(ctx context.Context, fields *PlayerFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookPlayer != nil {
			return CNHookPlayer(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringPlayer(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &PlayerModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// Player
/* Player Returns metadata for an individual's account. */
// Path: /sc2/player/:accountId
var Player = bridgePlayer

