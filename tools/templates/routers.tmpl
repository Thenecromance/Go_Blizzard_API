package routers

import (
	"Unofficial_API/app"
	"net/http"

	"github.com/gin-gonic/gin"
)

func init() {

{{range .Apis}} app.Instance().RegisterRoute("{{.Method}}", "{{.GinPath}}",gin{{.Name}})
{{end}}
}

{{range .Apis}}func gin{{.Name}}(c *gin.Context){
    c.JSON(http.StatusOK,gin.H{})
}
{{end}}